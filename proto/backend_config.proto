syntax = "proto3";

package fproxy;

option go_package = "proto";

message BackendConfig {
  message Server {
    string addr = 1;
    string cert_file = 2;
    string key_file = 3;
    string client_ca_file = 4;
  }
  Server server = 1;

  string serve_path = 2;
}

