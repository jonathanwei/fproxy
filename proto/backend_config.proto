syntax = "proto3";

package fproxy;

import "proto/tls.proto";

option go_package = "proto";

message BackendConfig {
  message Server {
    string addr = 1;
    oneof security {
      TLSConfig tls = 2;
      bool insecure = 3;
    }
  }
  Server server = 1;

  string serve_path = 2;
}

